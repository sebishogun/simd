# Working on this repository

Rules for agents and contributors working on `github.com/sebishogun/simd`.
They are deliberately short; the documents they point at carry the detail.
CLAUDE.md carries the same rules for Claude sessions.

## Product boundary and non-goals

This repository ships one thing: a Go package of whole-slice SIMD primitives
for numeric, text, and columnar work, with runtime-selected generated kernels
and a portable Go path for every operation.

It is not a BLAS, tensor library, dataframe, autodiff framework, or engine.
It does not own data models, allocation policy, or control flow — the caller
does. There is no cgo, no C toolchain needed to consume it, no JIT, and no
build tag required for the ordinary build (an optional `purego` tag exists for
portable-only builds and auditing). The one transitive dependency is
`golang.org/x/sys` (CPU feature detection). Higher-level projects built on the
library live in their own repositories.

## Current shipped status

The tip describes the shipped v1 line. The current release is **v1.20.0**:
493 exported functions, 6,931 generated kernels across nine tier targets
(sse2, avx2, avx512, neon, sve2, rvv, vx, lasx, vsx). The API is stable for
the life of v1: every exported function keeps its name, signature, and
meaning, and so does the numerical contract in `internal/kernel/kernel.go`.

Anything not shipped yet belongs in ROADMAP.md and in plans under
`docs/plans/` — see the roadmap-not-shipped rule below. Never describe future
work as shipped, and never write the README to describe anything other than
the current v1 behavior.

## Required reading, in order

1. `README.md` — the technical front page: what the package is, API model,
   accuracy contract, measured performance, platform summary.
2. `docs/architecture.md` — components, data flow, and generator source of
   truth.
3. `docs/lld/api-and-memory.md` — operation families, allocation, sizing,
   aliasing, and length contracts at the implementation level.
4. `docs/lld/kernels-and-dispatch.md` — CPU detection, tier selection,
   dispatch, linking, and hot-loop rules.
5. `docs/lld/generation-and-platforms.md` — the C-to-assembly pipeline and
   what each architecture/tier claim means.
6. `ROADMAP.md` — what is not built yet and why.
7. `docs/verification.md` — every gate, what it runs, and when it applies.
8. `docs/wrong.md` — measurements that rejected changes; read it before
   proposing anything.
9. `docs/plans/2026-08-13-simd-production-design.md` — the production design
   record and governance for future work.
10. `docs/plans/2026-08-13-simd-production.md` — the staged implementation
    plan for the remaining roadmap work.

Then, for the task at hand: `docs/api.md` before touching the public API,
`docs/kernels.md` before adding a kernel, `docs/runner.md` before touching
CI, and `docs/platforms.md` before making a platform claim.

## Package, ownership, and compatibility rules

- The root package `simd` is the public API, one file per topic. Its
  exported names are a v1 promise: do not rename, repurpose, or re-sign
  them, and do not change the numerical contract.
- `internal/kernel` is the contract every backend implements (the six rules,
  `SumLanes`, `CombineTree`). It is the most valuable prose in the
  repository; keep it exact.
- `internal/ref` is the portable specification, not a fallback. Every
  generated kernel is differentially tested against it; where they disagree,
  the kernel is wrong unless the bug is in the reference.
- `csrc/` is the source of truth for kernels; `internal/<arch>/*.s` is
  generated output and must not be edited by hand. `tools/` is a separate
  module that must never become a consumer dependency.
- Generated files (`dispatch_tables_<arch>.go`, `kernel_thresholds_<arch>.go`,
  the `.s` files) are regenerated by `make codegen`, never patched.
- Documentation counts and links are checked by tests
  (`internal/tests/docs`); keep every count and every local link true.

## Disassemble first, always

Before proposing a cause for anything slow, before writing a variant, before
reading a profile delta — build it and read the instructions:

```
go test -c -o /tmp/x.test .
go tool objdump -s 'pkg\.functionName' /tmp/x.test | less
```

Use gdb when a breakpoint or a live register is needed. Go compiles in
seconds; every guess that skips the disassembly costs a build-measure-revert
cycle and risks a wrong conclusion landing in `docs/wrong.md` as fact.

## Benchmark rules

The code-layout noise floor here is **8.3%**. Anything smaller cannot be told
from nothing by wall-clock, and more samples do not help — layout noise is
per-build, not per-run. When a change is expected to be worth less than that:

- compare **instructions retired** and **cycles** with `perf stat -e
  instructions:u,cycles:u`, which are layout-independent;
- and read the disassembly, which is the only thing that explains *why*.

A/B builds must be **interleaved** in one session and compared on the
minimum, never across sessions. Run the machine quiet: wait for load average
under 1.

## Gates: run them bare

**Never pipe a gate through `tail`** (or anything else) without `pipefail`:
the pipe reports the last command's status and the failure vanishes. This has
laundered a red fuzz run, a red README gate, and two red bench-check runs
into green exits. Run gates bare, or `set -o pipefail` first.

## Verification and release gates

Before any commit touching code or docs, at minimum:

- `go test ./...`
- `make verify` (fmt-check, vet, tests, purego build, every tier this CPU
  can run)
- `make check-emission` (generator inventory dry run)
- `git diff --check`

A release runs the full set: `make verify`, `make test-cross`,
`make test-riscv64`, `make test-loong64`, `make test-gates`, `make fuzz`,
and `make bench-check`, with the docs gates (`go test ./internal/tests/docs`)
green and every stable tag present in CHANGELOG.md. All the commands and
their roles are in `docs/verification.md`.

## The record

`docs/wrong.md` holds measurements that argued against changes, including
changes that were then reverted. A finding that cost a measurement belongs
there whether or not any code changed — the entry is the deliverable. It is a
historical record: never rewrite entries to read like current reference
pages.

## Roadmap-not-shipped rule

ROADMAP.md is the canonical roadmap. Nothing in it is a promise of a date,
and nothing in it is shipped until it is in the code, the tests, and the
changelog. Do not create a parallel roadmap; do not write prose that makes an
open item sound built. Future work lives in `docs/plans/` and is executed
task-by-task with the executing-plans workflow when it is actually taken on.
